defmodule GenioTestWeb.WizardView do
  use GenioTestWeb, :view

  #def render("wizard.html", assigns) do
    #template_name = assigns.changeset.data.__struct__
                    #|> to_string()
                    #|> String.replace("Elixir.GenioTest.Quote.", "")
                    #|> String.downcase()

    #template = EEx.eval_file("lib/genio_test_web/templates/wizard/quoting.html.eex", assigns: assigns)
    #require IEx;IEx.pry

     ##Map.replace!(params, :view_template, "#{template_name}.html")
     ##render(params.conn, GenioTestWeb.WizardView, "#{template_name}.html", changeset: params.changeset)
    #~E(<script src="file.js"></script>)
  #end
end
